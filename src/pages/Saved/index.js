import React from 'react';
import { View, Text } from 'react-native';

const Saved = () => ({
  render() {
    return (
      <View>
        <Text>Saved page</Text>
      </View>
    );
  },
});

export default Saved;
